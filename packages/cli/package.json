{
  "name": "@osb/cli",
  "version": "0.1.0",
  "description": "osb: command-line interface for the Obsidian second brain engine.",
  "type": "module",
  "bin": {
    "osb": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@osb/core": "workspace:*",
    "@osb/memory-node": "workspace:*",
    "commander": "^13.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
