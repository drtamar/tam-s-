{
  "name": "obsidian-second-brain",
  "version": "0.1.0",
  "private": true,
  "description": "Obsidian second brain: connect a vault as an AI memory graph, with a shared core engine, CLI, and MCP server.",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
